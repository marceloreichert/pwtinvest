<div class='list'>
	<div id='tickets'>
	  <% unless @papers.empty? %>
   		<%= render "paper", :collection => @papers %>
	  <% else %>
	    <%= render "common/empty" %>
		<% end %>
	</div>
</div>
<% content_for :sidebar, render(:partial => '/home/sidebar') -%>
